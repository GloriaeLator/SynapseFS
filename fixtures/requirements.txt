# Fixture generation only. NOT needed to build or run sfs.
#
# Pinned because the point of a fixture is that it was written by someone
# else's writer: if these versions drift, the header bytes drift, and a test
# that passes here may not pass against the evaluator's checkpoints.
numpy==2.1.3
torch==2.5.1
safetensors==0.4.5
